function [u] = delta(n)
%delta vecteur n en entrée
%   return l'impulsion unité discrète en sortie
u = [];

for i=1:length(n)
    if n(i)==0
        %u=[u 0];
        u(i)=1;
    else
        %u=[u 0];
        u(i)=0;
    end
end
end
